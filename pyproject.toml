[project]
name = "lk-agent"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "livekit-agents[deepgram,elevenlabs,groq,openai,silero,turn-detector]~=1.5",
    "livekit-plugins-noise-cancellation~=0.2",
    "openai>=2.7",
    "python-dotenv>=1.1.1",
]
